regex a:x a y:a z;
substitute symbol A for a
write att
echo --
sigma net
write att
echo --

regex a:x a y:a z;
substitute symbol A alpha for a
write att
echo --
sigma net
write att
echo --

regex a:x a y:a z;
substitute symbol a for a
write att
echo --
sigma net
write att
echo --

regex a:x a y:a z;
substitute symbol NOTHING for a
write att
echo --
sigma net
write att
echo --

regex a:x a y:a z;
substitute symbol bar for foo
write att
echo --
sigma net
write att
echo --

regex a:x a y:a z;
substitute symbol ? for a
write att
echo --
sigma net
write att
echo --
